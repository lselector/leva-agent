[project]
name = "mini-claw"
version = "0.1.0"
description = "Minimal local OpenClaw-style agent"
requires-python = ">=3.11"
dependencies = [
  "python-dotenv>=1.0",
  "httpx>=0.27",
  "openai>=1.0",
]
